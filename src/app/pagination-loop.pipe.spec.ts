import { PaginationLoopPipe } from './pagination-loop.pipe';

describe('PaginationLoopPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationLoopPipe();
    expect(pipe).toBeTruthy();
  });
});
